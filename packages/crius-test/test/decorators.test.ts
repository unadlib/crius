import {
  autorun,
  title,
  examples,
  beforeEach,
  afterEach,
  plugins,
} from '../src/decorators';

test('test @autorun', () => {

});

test('test @title', () => {
  
});

test('test @examples', () => {
  
});

test('test @beforeEach', () => {
  
});

test('test @afterEach', () => {
  
});

test('test @plugins', () => {
  
});
